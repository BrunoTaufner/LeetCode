class Solution:
    def romanToInt(self, s: str) -> int:
        numbers = ['I', 'V', 'X', 'L', 'C', 'D', 'M']
        num = 0
        
        for c in s:

        return num


print(Solution().romanToInt('XV'))